import assert from "node:assert/strict";
import { readFile } from "node:fs/promises";
import test from "node:test";
const manifest=JSON.parse(await readFile(new URL("../widget.manifest.json",import.meta.url)));
test("client widget is read-only and permissionless",()=>{assert.deepEqual(manifest.binding_modes,["read"]);assert.deepEqual(manifest.security.permissions,[]);assert.ok(manifest.capability_requirements.includes("clients_online"));assert.ok(manifest.capability_requirements.includes("clients"));});
test("client widget uses a glance-first, theme-aware presentation",async()=>{const source=await readFile(new URL("../src/widget.js",import.meta.url),"utf8");assert.match(source,/background:transparent/);assert.match(source,/data\.states/);assert.match(source,/Devices & security/);assert.match(source,/Devices online/);assert.doesNotMatch(source,/Recently seen devices|recentNode|clientsNode/);assert.doesNotMatch(source,/Clients online|Recently observed clients|Live inventory data|Updated just now/);assert.match(source,/grid-auto-rows:minmax\(52px,auto\)/);assert.match(source,/--piphi-widget-font-size-label/);assert.match(source,/--piphi-widget-font-size-value/);assert.match(source,/font-size:var\(--pfsense-label-size\)/);assert.match(source,/font-size:var\(--pfsense-value-size\)/);assert.match(source,/main\[data-state="live"\] \[role=status\]\{display:none\}/);assert.match(source,/ResizeObserver/);assert.match(source,/host\.setHeight\(Math\.ceil\(card\.scrollHeight\)\)/);assert.equal(manifest.layout.default_height,160);assert.ok(manifest.layout.max_height<=260);});
